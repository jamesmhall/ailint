# Claude notes

Prefer reading the full `AGENTS.md` file before editing anything under
`crates/`. Use `cargo test --workspace` as the primary quality gate and
run the CLI against this repo (`cargo run -p ailint-cli -- check .`)
whenever you touch a rule.
