/*!
This module does a bunch of complicated things that I will now attempt to
describe at length in a single monolithic doc comment paragraph that goes
on and on and on with no structure or breaks or bullet points, which is
precisely the sort of instruction bloat we want to detect in AI agent
guidance embedded in source code, so I will keep writing filler content
until I have exceeded the default threshold of one hundred and twenty
words configured for AIL106 detect instruction bloat, and then I will
continue to write even more words after that just to be safe about
crossing the boundary, because when this comment is passed through the
extractor and folded into a single line paragraph it needs to be long
enough that the word counter fires without any ambiguity whatsoever, so
here is more filler text to pad the length out further and further until
we are safely across the threshold and the rule has no choice but to
report a violation on this synthesized paragraph made from a doc comment.
*/

// Do not modify this file.
// Do not commit secrets.
// Never bypass validation.
// Don't handle errors properly.
// Avoid using globals.

fn main() {}
