# Good scoped-tools fixture

You may use the following tools:

- `cargo build` for compilation.
- `cargo test` for running the test suite.
- `git status` for inspecting the working tree.

Ask before running anything not in this list.
